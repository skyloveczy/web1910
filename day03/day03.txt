1. HTTPS和SSH
  推送时采用不同的身份认证的方式
  HTTPS每次都需要输入用户名和密码
  而SSH不需要输入用户名和密码

  使用SSH方式需要配置密钥:
    查看本机有没有密钥:
      ls -a ~/.ssh

    生成ssh密钥:
      ssh-keygen -t rsa -C "邮箱地址"

    生成2个文件:
      id_rsa : 私钥,保存在本机
      id_rsa.pub : 公钥,将其内容配置到github上

    打开公钥,复制里面的内容

